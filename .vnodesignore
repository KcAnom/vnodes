# Indexed-but-not-source. These are committed on purpose and still have no place
# in a dependency map: the map would draw its own bundle as a file of the
# project, and a lockfile as a node with one symbol.
#
# Not .gitignore, because these are tracked — the built map ships with the repo
# so nobody needs a toolchain to use vnodes.
src/view/static/
ui/package-lock.json

# Headless-Chrome scratch written by the repo's own ui/scripts/shots.sh
# (--user-data-dir). 181 of 232 indexed files lived here, and they ranked into
# real capsules: one run spent 2,196 of its 8,000 tokens on a Chrome
# bindings_main.js. A rendering check must not poison the index it is checking.
ui/.shots/
ui/.probe/
